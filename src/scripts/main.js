$(function(){
  $('.portfolio__slider').slick({
    centerMode: true,
    centerPadding: '210px',
    slidesToShow: 1,
    dots: true,
    arrows: false,
    autoplay: true,
      // slidesToShow: 5,
      // slidesToScroll: 1,

  });
});